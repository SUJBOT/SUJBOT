# ====================================================================
# MY_SUJBOT - API Keys Template
# ====================================================================
# UPDATED 2025-11-10: Configuration moved to config.json
# This file now contains ONLY API key templates
# Copy this to .env and fill in your actual keys
# ====================================================================
#
# For ALL other configuration, see config.json.example:
#   cp config.json.example config.json
#   # Edit config.json with your settings
# ====================================================================

# Anthropic Claude API (REQUIRED if using Claude models)
# Get from: https://console.anthropic.com/
ANTHROPIC_API_KEY=sk-ant-...

# OpenAI API (REQUIRED if using GPT models or OpenAI embeddings)
# Get from: https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-proj-...

# Voyage AI API (optional - only if using Voyage embeddings)
# Get from: https://www.voyageai.com/
VOYAGE_API_KEY=

# Google Gemini API (optional - only if using Gemini models)
# Get from: https://aistudio.google.com/apikey
GOOGLE_API_KEY=

# ====================================================================
# POSTGRESQL DATABASE (for Docker deployment)
# ====================================================================
# These values are used by docker-compose.yml
# IMPORTANT: Generate a secure password before deploying!
#
# Generate secure password (Linux/macOS):
#   openssl rand -base64 32
#
# Generate secure password (Python):
#   python -c "import secrets; print(secrets.token_urlsafe(32))"

POSTGRES_USER=postgres
POSTGRES_PASSWORD=  # REQUIRED: Set secure password here (min 20 chars)
POSTGRES_DB=sujbot
POSTGRES_PORT=5432

# Database URL (constructed from above values)
# Format: postgresql://USER:PASSWORD@HOST:PORT/DATABASE
# Replace YOUR_SECURE_PASSWORD with the value from POSTGRES_PASSWORD above
DATABASE_URL=postgresql://postgres:YOUR_SECURE_PASSWORD@postgres:5432/sujbot

# ====================================================================
# NEO4J (optional - if using Neo4j instead of Apache AGE)
# ====================================================================
# Note: Apache AGE in PostgreSQL is recommended (no separate service needed)

# NEO4J_URI=neo4j+s://your-instance.databases.neo4j.io
# NEO4J_USERNAME=neo4j
# NEO4J_PASSWORD=

# ====================================================================
# DOCKER CONFIGURATION
# ====================================================================

# Build target: "development" or "production"
BUILD_TARGET=development

# Backend port
BACKEND_PORT=8000

# Frontend ports
FRONTEND_DEV_PORT=5173  # Vite dev server (development)
FRONTEND_PROD_PORT=80   # Nginx (production)

# API base URL for frontend
VITE_API_BASE_URL=http://localhost:8000

# Logging level
LOG_LEVEL=INFO

# ====================================================================
# LANGSMITH OBSERVABILITY (optional)
# ====================================================================
# Enable in config.json: multi_agent.langsmith.enabled = true

# LANGSMITH_API_KEY=lsv2_pt_...
# LANGSMITH_PROJECT_NAME=sujbot2-multi-agent
