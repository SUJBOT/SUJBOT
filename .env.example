# API Keys Configuration
# Both keys can be set simultaneously:
# - ANTHROPIC_API_KEY: Used for LLM operations (Phase 2: Summarization)
# - OPENAI_API_KEY: Used for embeddings (Phase 4: Embedding, future)

# Claude API key (default provider for summarization)
ANTHROPIC_API_KEY=your_anthropic_api_key_here

# OpenAI API key (for embeddings and optional LLM fallback)
OPENAI_API_KEY=your_openai_api_key_here

# Data Paths (optional, defaults used if not set)
# DATA_DIR=data
# OUTPUT_DIR=output
